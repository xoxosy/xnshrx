const Discord = require('discord.js');
const client = new Discord.Client();
const moment = require('moment');
client.on('ready', () => {
  console.log(`Logged in as ${client.user.tag}!`);
});

client.on('message', msg => {
  if (msg.content === 'ping') {
    msg.reply('Pong!');
  }
});

client.login('NDU5Njk5Mjk3NzAxODU1MjMy.XO8mzw.c7vlONpwpUBVbivQGzN-q3gH1gY');


client.on("guildMemberAdd", member => {
  member.createDM().then(function (channel) {
  return channel.send(`ممكن تدخل لهون حبيبي ♥
  https://discord.gg/KZaHrfB`) 
}).catch(console.error)
})

const a = new Discord.Client();
const s = new Discord.Client();
const d = new Discord.Client();
const f = new Discord.Client();
const g = new Discord.Client();
const h = new Discord.Client();
const j = new Discord.Client();
const k = new Discord.Client();
const l = new Discord.Client();
const z = new Discord.Client();
const x = new Discord.Client();
const c = new Discord.Client();
const v = new Discord.Client();
const b = new Discord.Client();
const n = new Discord.Client();
const m = new Discord.Client();
const q = new Discord.Client();
const w = new Discord.Client();
const e = new Discord.Client();
const aa = new Discord.Client();
const ss = new Discord.Client();
const dd = new Discord.Client();
const ff = new Discord.Client();
const gg = new Discord.Client();
const hh = new Discord.Client();
const jj = new Discord.Client();
const kk = new Discord.Client();
const ll = new Discord.Client();
const zz = new Discord.Client();
const xx = new Discord.Client();
const cc = new Discord.Client();
const vv = new Discord.Client();
const bb = new Discord.Client();
const nn = new Discord.Client();
const mm = new Discord.Client();
const qq = new Discord.Client();
const ww = new Discord.Client();
const ee = new Discord.Client();
const rep = "#rep <@401445907239010314>";
const amr = "";
    const ownerid = '401445907239010314';
a.login("NDUxMjA1ODA1MDA2NjUxNDA0.De-Zzg.38wT58gF_srIarMBO30hvGcuamI");
s.login("NDUxNTU3NDYyMjgyMzM4MzA0.DfDhXQ.s3-5z9GhEOGegC-Eyqdj1_SMcWM");
d.login("NDUwMzA2NTkzNjExODQxNTM5.DexUog.dbVHDQL959e2quW1_PCiWYH-4Rs");
f.login("MzA4Mzg5MDk0MTk3NjkwMzY4.DexSaw.m56fziHoCVgl4zaMMfavr4vffnM");
g.login("NDUxMTU4MTI2NDkwNjgxMzQ1.De9tag.IsTpkhQzucFa6VRlVYmZIzKi4rk");
h.login("NDUxMTU5NTc0OTA1MzU2MzE4.De9uyA.QoBXMgxHMfuIIr5EJ-vLO4nbS58");
j.login("NDUxMTYwNjU5MTcwODIwMDk2.De9vyg.vXwIY8s9vyZKPH4cz35PbvOEFRg");
k.login("NDUxMTU3MDg0ODQzODAyNjM4.De9sVg.dhpUCjCX_hYjRgN-9OCswLwYIWI");
l.login("Mjk3ODg3MTE1Nzk3MDY5ODI1.De-avQ.hXRQfVWtv-AFjFCcpo3tNdbW8E4");
z.login("NDUxMjA2NDkzNjkwMjY1NjAw.De-ahQ.Ygxm8Iq1jCJdD2UMIUxCMAr5qZE");
x.login("NDUxMjA3NjA0NTYwMTk5Njgw.De-bXw.ZCmJFOw6_VCjjXkiQo_yOjdjsTk");
c.login("NDYwMTgwNTg1MTM1MDc5NDI0.DhBALg.PQodz030A8i7vHrBk1w4oSPi9Lc");
v.login("NDYwMTgxNTgyMzkzNzY5OTg1.DhBB3A.U_j5IGOxo8fw0C3F_G4SXT-03bY");
b.login("NDYwMTgzOTIzNzIxMjQwNTk3.DhBE-w.IktSMVlIIAlfZn_70MMTppY-nWU");
n.login("NDYwMTg2NjUwOTk2MTEzNDA4.DhBF7A.84nejNYNRPhIevR6xliTBymhB2E");
m.login("NDYwMTkxOTYyNDI3OTQ5MDU2.DhBK4w.LfU4n-yzaheb0bQKjtm2kSWS4wc");
q.login("NDYwMTkzMTcyNDg0Nzg0MTI4.DhBL3w.MYSvw28nY5tyhKRcRUpsYbcrwRQ");
w.login("NDYwMTk0NjExODUyMzQ1MzQ0.DhBNbQ.6JNOq1o8t6DguEuAjAWT_uHxrJc");
e.login("NDYwMTk5NzcwODI5NjE5MjIw.DhBSEw.99wjK0XSlZoLrJ8rdO16Rr9tLUQ");
aa.login("NDY0NDM4NzAwNjg0ODY5Njcy.Dh-99g.zrn6BX6gURc1HvwHfgXFOmM6l0I");
ss.login("NDY0NDQwMjg1NzIwNDc3Njk2.Dh-_OQ.4sOg-b8jo_vxA24vChHmxBzpOSA");
dd.login("NDY0NDQ1Nzg3MzQ3MDkxNDU2.Dh_FRw.39b4DqeJjkHK7kkoYVexxITAj8o");
ff.login("NDY0NDQ4NDU1MDgzODE5MDE5.Dh_G8Q.bdSMpXKuVUvp1F7FzkWFXLSFPd0");
gg.login("NDY0NDQ5MzU0MDM2NTQzNDg4.Dh_H1A.UY1Vtu7-XjnO6gtBOXJYlTe6Z60");
hh.login("NDY0NDQ5NzEwOTA0ODM2MTA2.Dh_Iaw._kyurPFDW4-aEf_B8OKJnwq_0jg");
jj.login("NDY0NDU4MzEwOTYwMjgzNjU4.Dh_QMg.gk7HuFw_OgZQid1fYhgg5E0A9Po");
kk.login("NDY0NDU4ODIzOTE5MzM3NDc4.Dh_Qvw.lFJh_mlDxChSRkE6XWsOK6oQzPQ");
ll.login("NDY0NDU5NTExMjQwMDY1MDI0.Dh_RUA.MOQCRPe5QUD2oWC2Hu8f9EzM6oQ");
zz.login("NDY0NDU5OTA5MTcwMjAwNTc3.Dh_R3Q.MtiVKVcbtgqld0x8eQHBJ7MyWUg");
xx.login("NDY0NDYwNTE1OTkzODQ1Nzcy.Dh_Sow.BYXqDfAbkFrnpImpUm06EkAp_lk");
cc.login("NDY0NDYxNzA0NTAwMjE1ODI1.Dh_TQw.GR17LZdBsnz8mc2kaGJTms0IYOg");
vv.login("NDY0NDYxOTEyNDQ5NDgyNzUz.Dh_TzA.H_vKoZbC26ZjYvmDozbPvU0U2kE");
bb.login("NDY0NDYyNTEyMDE2OTgyMDE2.Dh_UZw.s09h4OZUt6zNgrzAFDz7-5GBbhs");
nn.login("NDY0NDYzMjExMjgzMDg3Mzcw.Dh_U_g.UibT4JbDXCTL-EHJ7M4we5qhPoc");
mm.login("NDY0NDYzNzYyMjQ2ODYwODAw.Dh_Vmw.qdngRUmnTGStyZM2y3LDI-Noj-k");
qq.login("NDY0NDY0NzM5ODUxMDQyODI0.DiAi-A.z5LSqd1WNLKRwXAuCRMCD7em4B8");
ww.login("NDY0NTQ3NjM1Mzc4OTEzMzAy.DiAj2w.AhdecS7Ff4sY-HkqXda9gXE8fx0");
ee.login("NDYxNjQ3MTA5MDU4MzMwNjU0.DhWV4A.8E6rZWfpFz_2pYTP6BR6WsWHt_Y");
a.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
s.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;
    message.delete().catch(O_o=>{}); 

   message.channel.sendMessage(args.join("  "))
  }
});
d.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
f.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
g.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
h.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
j.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
k.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
l.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
z.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});

x.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});

c.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
v.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
b.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
n.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
m.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
q.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
w.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
e.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});

a.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '1') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
s.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '2') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
d.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '3') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
f.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '4') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
g.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '5') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});

h.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '6') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});

j.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '7') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});

k.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '8') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});

l.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '9') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});

z.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '10') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});

x.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '11') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});

c.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '12') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
v.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '13') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
b.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '14') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
n.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '15') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
m.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '16') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
q.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '17') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
w.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '18') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
e.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '19') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
/////////////////////////////////////////////
aa.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
ss.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;
    message.delete().catch(O_o=>{}); 

   message.channel.sendMessage(args.join("  "))
  }
});
dd.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
ff.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
gg.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
hh.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
jj.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
kk.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
ll.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
zz.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});

xx.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});

cc.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
vv.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
bb.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
nn.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
mm.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
qq.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
ww.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});
ee.on('message', message => {
  if (message.author.bot) return;
  let command = message.content.split(" ")[0];

  let args = message.content.split(" ").slice(1);

  if (command == "say") {
        		        if (message.author.id != ownerid) return;

   message.channel.sendMessage(args.join("  "))
  }
});

aa.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '1') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
ss.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '2') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
dd.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '3') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
ff.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '4') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
gg.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '5') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});

hh.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '6') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});

jj.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '7') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});

kk.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '8') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});

ll.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '9') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});

zz.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '10') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});

xx.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '11') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});

cc.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '12') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
vv.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '13') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
bb.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '14') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
nn.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '15') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
mm.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '16') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
qq.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '17') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
ww.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '18') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});
ee.on('message', message => {
  // Voice only works in guilds, if the message does not come from a guild,
  // we ignore it
  if (!message.guild) return;

  if (message.content === '19') {
    		        if (message.author.id != ownerid) return;

    // Only try to join the sender's voice channel if they are in one themselves
    if (message.member.voiceChannel) {
      message.member.voiceChannel.join()
        .then(connection => { // Connection is an instance of VoiceConnection
        })
        .catch(console.log);
    } else {
    }
  }
});



// THIS  MUST  BE  THIS  WAY


client.login(process.env.BOT_TOKEN);
